export type pizzaDataTypes = {
  pic: string;
  name: string;
  desc: string;
  price: number;
};

export type SectionTitleProps = {
  text: string;
  textColor?: string;
  borderColor?: string;
  className?: string;
};

export type PizzaProp = {
  pic: string;
  name: string;
  desc: string;
  price: string;
};
